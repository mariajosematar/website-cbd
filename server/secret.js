module.exports = {
    stripeSecret, // Stripe secret api key
    emailUsername, // This is the mailgun username
    emailPassword, // This is the mailgun password
    adminName,
    mongoUrl: 'mongodb://', // This can be public because it's just the user of the databas
}